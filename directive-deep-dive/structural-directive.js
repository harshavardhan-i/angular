// • There is only property binding, event binding, two way binding and string interpolation
// • * has to be converted into one of the above mentioned tools
// • So * basically converts the directive to the form
//     ○ ng-template [ngIf]="expression" with div within
//     ○ ng-template does not render in itself but it tells angular some of it's content needs to…
    
